<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GeekHubApp.MarioBrosPage"
             Title="Mario Bros">
  <ContentPage.Content>
    <StackLayout VerticalOptions="CenterAndExpand"
                 HorizontalOptions="CenterAndExpand"
                 Spacing="20"
                 Padding="30">
      <Label Text="Super Mario Bros"
             FontSize="32"
             FontAttributes="Bold"
             HorizontalOptions="Center" />
      <Image Source="mario.png"
             HeightRequest="200"
             WidthRequest="200"
             HorizontalOptions="Center" />
      <Label Text="¡Bienvenido al mundo de Mario Bros! Disfruta de la aventura."
             FontSize="18"
             HorizontalOptions="Center"
             HorizontalTextAlignment="Center" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>
